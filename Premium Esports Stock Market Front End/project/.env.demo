# Loaded automatically by `npm run dev:demo` (vite --mode demo), on top of
# whatever's in .env. Points the frontend at the demo backend (port 8001)
# instead of the real one (port 8000) -- see DEMO_MODE.md at the repo root.
VITE_API_BASE_URL=http://localhost:8001/api/v1
